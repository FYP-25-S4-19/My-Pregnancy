import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function DoctorProfileScreen() {
  return (
    <SafeAreaView edges={["top"]}>
      <Text>TODO: Doctor Profile Screen</Text>
    </SafeAreaView>
  );
}
