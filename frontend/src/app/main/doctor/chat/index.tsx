import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function DoctorChatListScreen() {
  return (
    <SafeAreaView edges={["top"]}>
      <Text>TODO: Doctor Chats Screen</Text>
    </SafeAreaView>
  );
}
