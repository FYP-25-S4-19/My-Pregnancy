import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function NutritionistProfileScreen() {
  return (
    <SafeAreaView edges={["top"]}>
      <Text>TODO: Nutritionist Profile Screen</Text>
    </SafeAreaView>
  );
}
