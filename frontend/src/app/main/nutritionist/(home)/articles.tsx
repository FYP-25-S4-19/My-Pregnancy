import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

export default function NutritionistArticlesScreen() {
  return (
    <SafeAreaView edges={["top"]}>
      <Text>TODO: Nutritionist Articles screen</Text>
    </SafeAreaView>
  );
}
