import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function MotherRecipeScreen() {
  return (
    <SafeAreaView edges={["top"]}>
      <Text>Mother recipe screen</Text>
    </SafeAreaView>
  );
}
